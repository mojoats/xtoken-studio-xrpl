{
  "name": "xtoken-studio-xrpl",
  "version": "0.1.0-alpha.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "pnpm": "^9.12.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
